matrix:
  platform:
    - ubuntu2204
  bazel:
    - 8.x

tasks:
  verify_build_targets:
    name: "PoC module A"
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
      - "//..."
