matrix:
  platform:
    - ubuntu2204
  bazel:
    - 8.x

tasks:
  verify_build_targets:
    name: "PoC module B"
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
      - "//..."
